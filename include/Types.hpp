#pragma once

namespace WakeOnLan {
    /**
     * @enum HandlerType
     * The API handler type.
     */
    enum HandlerType {
        Manager = 0,      ///< Manager handler option.
        Participant = 1   ///< Participant handler option.
    };
} // namespace WakeOnLan